<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <!-- Add your CSS and other head content here -->
</head>

<body>
    <header>
        <!-- Include your header layout here -->
    </header>
    <main>
        <!-- Other content of the home page -->
        
        <% if (query) { %>
            <h2>Search Results for "<%= query %>"<% if (category) { %> in "<%= category %>"<% } %>:</h2>
            <% if (results && results.length > 0) { %>
                <ul>
                    <% results.forEach(result => { %>
                        <li><%= result.name %> - <%= result.description %></li>
                    <% }); %>
                </ul>
            <% } else { %>
                <p>No results found</p>
            <% } %>
        <% } %>
    </main>
    <footer>
        <!-- Include your footer layout here -->
    </footer>
</body>

</html>
